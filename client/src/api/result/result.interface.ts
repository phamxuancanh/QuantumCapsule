export interface IResult {
    id?: string
    totalScore?: number
    userId?: string
    timeStart?: Date
    timeEnd?: Date
    subjectId?: string
    chapterId?: string
    lessonId?: string
    examId?: string
}